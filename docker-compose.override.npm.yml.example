version: '3.8'

services:
  chrome:
    environment:
      - PUID=${PUID:-1000}
      - PGID=${PGID:-1000}
      - TZ=${TZ:-Europe/Madrid}
    volumes:
      - chrome_config:/config
    networks:
      - npm

volumes:
  chrome_config:

networks:
  npm:
    external: true